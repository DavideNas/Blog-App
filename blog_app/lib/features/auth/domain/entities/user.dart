// Entity to map user table

class User {
  // ID
  final String id;
  // EMAIL
  final String email;
  // NAME
  final String name;
  User({required this.id, required this.email, required this.name});
}
